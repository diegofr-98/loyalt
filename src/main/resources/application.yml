spring:
  application:
    name: loyalt
  profiles:
    active: local
  devtools:
    restart:
      enabled: true
      poll-interval: 1s
      quiet-period: 400ms

  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
